1. Line 9 will print as follow: `values added: 20`<br>
2. Line 13 will print as follow: `final result: 20`<br>
3. Line 9 will print as follow: `values added: 20`<br>
4. Code returns an error, because the variable `result` is declared by the keyword `let` which means it can only be accessed within the block it is defined in.<br>
5. Code returns an error, because the variable `result` is declared by the keyword `const` so that it cannot be changed.
6. Code returns an error, because the variable `result` is declared by the keyword `const` so that it cannot be changed.